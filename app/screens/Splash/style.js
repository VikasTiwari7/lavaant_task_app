import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  wrapper: {
    flex: 1,
  },
  container: {
    flex: 1,
    width: null,
    alignSelf: 'stretch',
    height: null,
    alignItems: 'center',
    justifyContent: 'center',
  }
});
